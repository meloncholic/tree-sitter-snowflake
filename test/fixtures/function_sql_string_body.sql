CREATE OR REPLACE FUNCTION DB1.S2.F1("P1" VARCHAR)
RETURNS VARCHAR
LANGUAGE SQL
AS '
    CASE
        WHEN P1 IS NULL THEN NULL
        WHEN TRIM(P1) = '''' THEN NULL
        ELSE TRIM(REGEXP_REPLACE(UPPER(TRIM(P1)), ''[.,]'', ''''))
    END
';
